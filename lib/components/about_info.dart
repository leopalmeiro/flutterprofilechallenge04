import 'package:flutter/material.dart';

class AboutInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(10.0),
      child: Text('Over 15+ years of experience in ​​Technology area'),
    );
  }
}
